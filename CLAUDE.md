# HonestBaby 開発ルール（全セッション共通）

このファイルはリポジトリに含まれるため、どのセッションでも自動的に適用される。

## ワークフロー（本番反映まで完了させる）
- 修正・新機能の作業は、**作業ブランチ → `main` へマージ → `origin/main` へ push（＝Vercel 本番反映）** まで必ず完了させる。途中のブランチ push で止めない。
- マージ前に必ずローカルで `npm run build` が通ることを確認する。
- 本番反映後は Vercel のデプロイが `READY` になるまで確認し、可能なら本番URLで動作検証する（外部到達が必要な検証は Vercel MCP の `web_fetch_vercel_url` を使う）。
- 本番デプロイは Vercel が `main` への push で自動実行する。PR は明示的に依頼された場合のみ作成する。
- 上記は本番反映の標準許可とする（HonestBaby の作業に限り、毎回の確認は不要）。

## コミュニケーション
- 回答・説明は日本語。結果だけでなく過程（調査→判断→実施→検証）も説明する。

## SEO（修正・機能追加時は必ず配慮）
- ページは原則 SSR/ISR で配信し、クローラーに読めるHTMLを返す（このプロジェクトは App Router + ISR が基本）。
- 各ページに適切な `metadata`（title / description）と `alternates.canonical` を設定する。
- 商品・パンくず等は JSON-LD 構造化データ（Product / BreadcrumbList 等）を出力する。
- OG / Twitter カード画像は**絶対URL**で指定する（相対URLや同一オリジンプロキシ経由にしない）。
- `<img>` には意味のある `alt` を付ける。一覧は内部リンク（`<a>`/`next/link`）で辿れるようにする。
- `app/sitemap`・`robots`・canonical の整合を保ち、404/リダイレクトを正しく扱う。
- 表示速度（画像最適化・遅延読み込み・不要JS削減）に配慮する。SEOに直結する。

## セキュリティ（修正・機能追加時は必ず配慮）
- 秘密情報（APIキー/サービスロールキー等）をクライアントへ露出しない。ブラウザから直接外部APIを叩かず、サーバー(route handler)経由にする。`NEXT_PUBLIC_*` には機微情報を入れない。
- Supabase は RLS を前提に設計する。書き込みは範囲限定、管理操作はパスワード保護APIを経由させる。
- 外部リソースを取得するサーバー機能は、ホスト許可リストで SSRF を防ぐ（例: 画像プロキシは yimg.jp / rakuten.co.jp のみ許可）。
- 公開APIは入力検証とレート制限を行う。不要なワイルドカードCORSを付けない。
- セキュリティヘッダー（CSP / X-Content-Type-Options / X-Frame-Options 等）を維持・尊重する。新機能で必要な接続先は CSP に最小限だけ追加する。
- 外部から取得したデータ（API応答・ユーザー入力）は信頼せず、表示・保存前にサニタイズ/検証する。
